namespace DST.Core.LocalHourAngleDateTime
{
    // Provides values to determine with which cycle of a local hour angle (LHA) to track.
    public enum HourAngleCycle
    {
        Previous,
        Next
    }
}
