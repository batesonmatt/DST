namespace DST.Core.Coordinate
{
    // Provides values for formatting coordinates as a string.
    public enum FormatType
    {
        Decimal,
        Component,
        Compact,

        Default = Component
    }
}
